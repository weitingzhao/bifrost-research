name: bifrost_research
version: "0.5.0"
config-version: 2

profile: bifrost_research

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  bifrost_research:
    staging:
      +materialized: table
    intermediate:
      +materialized: table
    marts:
      +materialized: table
  # Elementary observability metadata (dbt package → ops_dbt schema)
  elementary:
    +schema: ops_dbt
